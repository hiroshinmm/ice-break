# 実装計画: 完全な要求仕様書の作成とドキュメント整理

## 概要
本プロジェクトの全工程と最新の技術仕様を統合し、最終的な「要求仕様書」を完成させます。これまでの個別の修正（AI画像生成、JST対応、レスポンシブ化など）を一つのマスタードキュメントに集約します。

## 実施内容
1.  **トピックフォルダの作成**: `docs/final_requirements_specification/` を作成。
2.  **要求仕様書の執筆**: `requirements_specification.md` を作成。以下の最新情報を反映。
    *   Pollinations.ai による 1024x1024 の AI 画像生成。
    *   Puppeteer のタイムアウト 90 秒設定（生成待ち対策）。
    *   1800px ワイドレイアウト & 2カラム/1カラムのメディアクエリ設計。
    *   `Asia/Tokyo` タイムゾーンによる JST 日付表示の強制。
    *   GitHub Actions による全自動パイプライン（Daily 8:00 JST）。
3.  **関連資料の移動/整理**: 過去のタスクや計画を本フォルダに集約。

## 検証プラン
- 内容に不足がないか、実際の `src/` 配下のコードと整合性が取れているかを確認します。
